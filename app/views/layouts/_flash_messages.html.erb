<% flash.each do |name, msg| %>
  <div class="alert alerts-<%= name == :notice ? "success" : "error" %>">
    <a class="close" data-dismiss="alert">x</a>
    <%= content_tag :div, msg, id: "flash_#{name}" if msg.is_a?(String) %>
  </div>  
<% end %>